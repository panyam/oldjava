package com.sri.java.types;

import java.io.*;

/**
 * Represents a type class.
 * Now a type can also be considered an expression.
 * For example, when it comes to do things like casting
 * we can have a casting operator that converts from one
 * type to another...  or the instanceof operator
 * works on normal expression and a Type expression.
 */
public abstract class Type 
{
}
